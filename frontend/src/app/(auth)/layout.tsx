/**
 * Layout per le pagine di autenticazione (login, registrazione).
 * Nessuna sidebar — layout a schermo intero.
 */
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
